# ──────────────────────────────────────────────────────────────────────────────
# [PT-BR] Arquivo .env.example — modelo de variáveis de ambiente (sem segredos).
# [EN]    .env.example file — template of environment variables (no secrets).
# [ES]    Archivo .env.example — plantilla de variables de entorno (sin secretos).
# [中文]   .env.example 模板 — 环境变量示例（不含敏感信息）。
# ──────────────────────────────────────────────────────────────────────────────

# [PT-BR] Provedor padrão (ollama = local/grátis; openai = nuvem).
# [EN]    Default provider (ollama = local/free; openai = cloud).
# [ES]    Proveedor por defecto (ollama = local/gratis; openai = nube).
# [中文]   默认提供方（ollama = 本地/免费；openai = 云端）。
PROVIDER=ollama

# --- Ollama ---------------------------------------------------------------
# [PT-BR] URL base; no docker-compose use "ollama:11434".
# [EN]    Base URL; in docker-compose use "ollama:11434".
# [ES]    URL base; en docker-compose use "ollama:11434".
# [中文]   基础地址；在 docker-compose 中使用 "ollama:11434"。
OLLAMA_BASE_URL=http://ollama:11434

# [PT-BR] Modelo sugerido: 1b (16 GB RAM) ou 3b se máquina for estável.
# [EN]    Suggested model: 1b (16 GB RAM) or 3b if machine runs smoothly.
# [ES]    Modelo sugerido: 1b (16 GB RAM) o 3b si la máquina va fluida.
# [中文]   推荐模型：1b（16 GB 内存）或在稳定时使用 3b。
OLLAMA_MODEL=llama3.2:1b

# [PT-BR] Timeout (s), contexto e número de tokens previstos.
# [EN]    Timeout (s), context size and number of predicted tokens.
# [ES]    Timeout (s), tamaño de contexto y número de tokens previstos.
# [中文]   超时（秒）、上下文窗口大小以及预测的 token 数量。
OLLAMA_TIMEOUT=180
OLLAMA_NUM_CTX=512
OLLAMA_NUM_PREDICT=64

# --- OpenAI ---------------------------------------------------------------
# [PT-BR] Descomente e configure se for usar OpenAI.
# [EN]    Uncomment and configure if you want to use OpenAI.
# [ES]    Descomentar y configurar si desea usar OpenAI.
# [中文]   若要使用 OpenAI，请取消注释并配置。
# PROVIDER=openai
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENAI_MODEL=gpt-4o-mini
# OPENAI_ORG_ID=
# OPENAI_PROJECT=
# OPENAI_SYSTEM_PROMPT="Você é um assistente útil."
